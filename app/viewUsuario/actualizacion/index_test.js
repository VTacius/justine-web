'use strict';

describe('myApp.usuario.actualizacion module', function() {

  beforeEach(module('myApp.usuario.actualizacion'));

  describe('usuario_actualizacion controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ViewUsuarioActualizacionCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
